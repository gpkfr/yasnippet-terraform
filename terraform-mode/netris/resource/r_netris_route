# -*- mode: snippet -*-
# name: r_netris_route
# key: r_netris_route
# --

resource "netris_route" "${1:this}" {
    description = "${2:description}"
    nexthop = "${3:nexthop}"
    prefix = "${4:prefix}"
    siteid = "${5:siteid}"
}