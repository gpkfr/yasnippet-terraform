# -*- mode: snippet -*-
# name: r_gitlab_user_custom_attribute
# key: r_gitlab_user_custom_attribute
# --

resource "gitlab_user_custom_attribute" "${1:this}" {
    key = "${2:key}"
    user = "${3:user}"
    value = "${4:value}"
}