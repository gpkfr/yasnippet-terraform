# -*- mode: snippet -*-
# name: r_gitlab_group_access_token
# key: r_gitlab_group_access_token
# --

resource "gitlab_group_access_token" "${1:this}" {
    group = "${2:group}"
    name = "${3:name}"
    scopes = "${4:scopes}"
}