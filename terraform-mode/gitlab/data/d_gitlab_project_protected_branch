# -*- mode: snippet -*-
# name: d_gitlab_project_protected_branch
# key: d_gitlab_project_protected_branch
# --

data "gitlab_project_protected_branch" "${1:this}" {
    name = "${2:name}"
    project_id = "${3:project_id}"
}