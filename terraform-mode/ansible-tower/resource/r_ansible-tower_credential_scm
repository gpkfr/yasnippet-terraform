# -*- mode: snippet -*-
# name: r_ansible-tower_credential_scm
# key: r_ansible-tower_credential_scm
# --

resource "ansible-tower_credential_scm" "${1:this}" {
    name = "${2:name}"
    organisation_id = "${3:organisation_id}"
}