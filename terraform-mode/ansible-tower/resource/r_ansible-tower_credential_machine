# -*- mode: snippet -*-
# name: r_ansible-tower_credential_machine
# key: r_ansible-tower_credential_machine
# --

resource "ansible-tower_credential_machine" "${1:this}" {
    name = "${2:name}"
    organisation_id = "${3:organisation_id}"
}