# -*- mode: snippet -*-
# name: r_gcore_k8s_pool
# key: r_gcore_k8s_pool
# --

resource "gcore_k8s_pool" "${1:this}" {
    cluster_id = "${2:cluster_id}"
    flavor_id = "${3:flavor_id}"
    max_node_count = "${4:max_node_count}"
    min_node_count = "${5:min_node_count}"
    name = "${6:name}"
    node_count = "${7:node_count}"
}