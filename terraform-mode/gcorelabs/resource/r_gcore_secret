# -*- mode: snippet -*-
# name: r_gcore_secret
# key: r_gcore_secret
# --

resource "gcore_secret" "${1:this}" {
    certificate = "${2:certificate}"
    certificate_chain = "${3:certificate_chain}"
    name = "${4:name}"
    private_key = "${5:private_key}"
}