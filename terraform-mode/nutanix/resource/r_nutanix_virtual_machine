# -*- mode: snippet -*-
# name: r_nutanix_virtual_machine
# key: r_nutanix_virtual_machine
# --

resource "nutanix_virtual_machine" "${1:this}" {
    cluster_uuid = "${2:cluster_uuid}"
    name = "${3:name}"
}