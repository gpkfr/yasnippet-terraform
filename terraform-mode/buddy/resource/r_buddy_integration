# -*- mode: snippet -*-
# name: r_buddy_integration
# key: r_buddy_integration
# --

resource "buddy_integration" "${1:this}" {
    domain = "${2:domain}"
    name = "${3:name}"
    scope = "${4:scope}"
    type = "${5:type}"
}