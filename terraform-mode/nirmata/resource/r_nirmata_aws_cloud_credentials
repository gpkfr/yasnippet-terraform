# -*- mode: snippet -*-
# name: r_nirmata_aws_cloud_credentials
# key: r_nirmata_aws_cloud_credentials
# --

resource "nirmata_aws_cloud_credentials" "${1:this}" {
    access_type = "${2:access_type}"
    name = "${3:name}"
    region = "${4:region}"
}