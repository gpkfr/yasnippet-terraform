# -*- mode: snippet -*-
# name: r_ionoscloud_ipfailover
# key: r_ionoscloud_ipfailover
# --

resource "ionoscloud_ipfailover" "${1:this}" {
    datacenter_id = "${2:datacenter_id}"
    ip = "${3:ip}"
    lan_id = "${4:lan_id}"
    nicuuid = "${5:nicuuid}"
}