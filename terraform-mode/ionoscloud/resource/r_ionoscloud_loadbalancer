# -*- mode: snippet -*-
# name: r_ionoscloud_loadbalancer
# key: r_ionoscloud_loadbalancer
# --

resource "ionoscloud_loadbalancer" "${1:this}" {
    datacenter_id = "${2:datacenter_id}"
    name = "${3:name}"
    nic_ids = "${4:nic_ids}"
}