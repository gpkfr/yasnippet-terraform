# -*- mode: snippet -*-
# name: r_venafi_ssh_certificate
# key: r_venafi_ssh_certificate
# --

resource "venafi_ssh_certificate" "${1:this}" {
    key_id = "${2:key_id}"
    template = "${3:template}"
}