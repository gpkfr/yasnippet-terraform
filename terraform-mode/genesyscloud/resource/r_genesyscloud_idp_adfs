# -*- mode: snippet -*-
# name: r_genesyscloud_idp_adfs
# key: r_genesyscloud_idp_adfs
# --

resource "genesyscloud_idp_adfs" "${1:this}" {
    certificates = "${2:certificates}"
    issuer_uri = "${3:issuer_uri}"
}