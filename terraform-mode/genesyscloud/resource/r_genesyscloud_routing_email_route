# -*- mode: snippet -*-
# name: r_genesyscloud_routing_email_route
# key: r_genesyscloud_routing_email_route
# --

resource "genesyscloud_routing_email_route" "${1:this}" {
    domain_id = "${2:domain_id}"
    from_name = "${3:from_name}"
    pattern = "${4:pattern}"
}