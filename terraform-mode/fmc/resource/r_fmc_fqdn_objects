# -*- mode: snippet -*-
# name: r_fmc_fqdn_objects
# key: r_fmc_fqdn_objects
# --

resource "fmc_fqdn_objects" "${1:this}" {
    dns_resolution = "${2:dns_resolution}"
    name = "${3:name}"
    value = "${4:value}"
}