# -*- mode: snippet -*-
# name: r_dnsimple_record
# key: r_dnsimple_record
# --

resource "dnsimple_record" "${1:this}" {
    domain = "${2:domain}"
    name = "${3:name}"
    type = "${4:type}"
    value = "${5:value}"
}