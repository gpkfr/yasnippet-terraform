# -*- mode: snippet -*-
# name: r_sigsci_site_list
# key: r_sigsci_site_list
# --

resource "sigsci_site_list" "${1:this}" {
    entries = "${2:entries}"
    name = "${3:name}"
    site_short_name = "${4:site_short_name}"
    type = "${5:type}"
}