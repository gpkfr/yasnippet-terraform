# -*- mode: snippet -*-
# name: r_wavefront_external_link
# key: r_wavefront_external_link
# --

resource "wavefront_external_link" "${1:this}" {
    description = "${2:description}"
    name = "${3:name}"
    template = "${4:template}"
}