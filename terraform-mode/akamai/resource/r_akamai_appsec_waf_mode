# -*- mode: snippet -*-
# name: r_akamai_appsec_waf_mode
# key: r_akamai_appsec_waf_mode
# --

resource "akamai_appsec_waf_mode" "${1:this}" {
    config_id = "${2:config_id}"
    mode = "${3:mode}"
    security_policy_id = "${4:security_policy_id}"
    version = "${5:version}"
}