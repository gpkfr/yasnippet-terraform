# -*- mode: snippet -*-
# name: r_amplience_content_type_schema
# key: r_amplience_content_type_schema
# --

resource "amplience_content_type_schema" "${1:this}" {
    body = "${2:body}"
    schema_id = "${3:schema_id}"
    validation_level = "${4:validation_level}"
}