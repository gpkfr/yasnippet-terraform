# -*- mode: snippet -*-
# name: r_netapp-cloudmanager_aws_fsx_volume
# key: r_netapp-cloudmanager_aws_fsx_volume
# --

resource "netapp-cloudmanager_aws_fsx_volume" "${1:this}" {
    client_id = "${2:client_id}"
    file_system_id = "${3:file_system_id}"
    name = "${4:name}"
    size = "${5:size}"
    tenant_id = "${6:tenant_id}"
    unit = "${7:unit}"
    volume_protocol = "${8:volume_protocol}"
}