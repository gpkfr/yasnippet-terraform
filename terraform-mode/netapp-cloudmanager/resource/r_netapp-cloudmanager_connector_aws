# -*- mode: snippet -*-
# name: r_netapp-cloudmanager_connector_aws
# key: r_netapp-cloudmanager_connector_aws
# --

resource "netapp-cloudmanager_connector_aws" "${1:this}" {
    company = "${2:company}"
    iam_instance_profile_name = "${3:iam_instance_profile_name}"
    key_name = "${4:key_name}"
    name = "${5:name}"
    region = "${6:region}"
    security_group_id = "${7:security_group_id}"
    subnet_id = "${8:subnet_id}"
}