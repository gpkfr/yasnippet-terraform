# -*- mode: snippet -*-
# name: r_awscc_acmpca_certificate
# key: r_awscc_acmpca_certificate
# --

resource "awscc_acmpca_certificate" "${1:this}" {
    certificate_authority_arn = "${2:certificate_authority_arn}"
    certificate_signing_request = "${3:certificate_signing_request}"
    signing_algorithm = "${4:signing_algorithm}"
    validity = "${5:validity}"
}