# -*- mode: snippet -*-
# name: r_awscc_autoscaling_launch_configuration
# key: r_awscc_autoscaling_launch_configuration
# --

resource "awscc_autoscaling_launch_configuration" "${1:this}" {
    image_id = "${2:image_id}"
    instance_type = "${3:instance_type}"
}