# -*- mode: snippet -*-
# name: r_awscc_qldb_stream
# key: r_awscc_qldb_stream
# --

resource "awscc_qldb_stream" "${1:this}" {
    inclusive_start_time = "${2:inclusive_start_time}"
    kinesis_configuration = "${3:kinesis_configuration}"
    ledger_name = "${4:ledger_name}"
    role_arn = "${5:role_arn}"
    stream_name = "${6:stream_name}"
}