# -*- mode: snippet -*-
# name: r_awscc_efs_mount_target
# key: r_awscc_efs_mount_target
# --

resource "awscc_efs_mount_target" "${1:this}" {
    file_system_id = "${2:file_system_id}"
    security_groups = "${3:security_groups}"
    subnet_id = "${4:subnet_id}"
}