# -*- mode: snippet -*-
# name: r_panos_ospf_area_interface
# key: r_panos_ospf_area_interface
# --

resource "panos_ospf_area_interface" "${1:this}" {
    name = "${2:name}"
    ospf_area = "${3:ospf_area}"
    virtual_router = "${4:virtual_router}"
}