# -*- mode: snippet -*-
# name: r_artifactory_local_generic_repository
# key: r_artifactory_local_generic_repository
# --

resource "artifactory_local_generic_repository" "${1:this}" {
    key = "${2:key}"
}