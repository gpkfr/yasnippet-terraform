# -*- mode: snippet -*-
# name: r_artifactory_artifact_property_webhook
# key: r_artifactory_artifact_property_webhook
# --

resource "artifactory_artifact_property_webhook" "${1:this}" {
    event_types = "${2:event_types}"
    key = "${3:key}"
    url = "${4:url}"
}