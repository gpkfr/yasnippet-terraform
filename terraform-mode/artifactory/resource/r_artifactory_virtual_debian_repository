# -*- mode: snippet -*-
# name: r_artifactory_virtual_debian_repository
# key: r_artifactory_virtual_debian_repository
# --

resource "artifactory_virtual_debian_repository" "${1:this}" {
    key = "${2:key}"
}