# -*- mode: snippet -*-
# name: r_artifactory_virtual_rpm_repository
# key: r_artifactory_virtual_rpm_repository
# --

resource "artifactory_virtual_rpm_repository" "${1:this}" {
    key = "${2:key}"
}