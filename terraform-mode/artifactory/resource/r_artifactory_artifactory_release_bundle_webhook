# -*- mode: snippet -*-
# name: r_artifactory_artifactory_release_bundle_webhook
# key: r_artifactory_artifactory_release_bundle_webhook
# --

resource "artifactory_artifactory_release_bundle_webhook" "${1:this}" {
    event_types = "${2:event_types}"
    key = "${3:key}"
    url = "${4:url}"
}