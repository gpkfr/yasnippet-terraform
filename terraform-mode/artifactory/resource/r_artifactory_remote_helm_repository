# -*- mode: snippet -*-
# name: r_artifactory_remote_helm_repository
# key: r_artifactory_remote_helm_repository
# --

resource "artifactory_remote_helm_repository" "${1:this}" {
    key = "${2:key}"
    url = "${3:url}"
}