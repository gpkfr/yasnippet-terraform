# -*- mode: snippet -*-
# name: r_artifactory_remote_generic_repository
# key: r_artifactory_remote_generic_repository
# --

resource "artifactory_remote_generic_repository" "${1:this}" {
    key = "${2:key}"
    url = "${3:url}"
}