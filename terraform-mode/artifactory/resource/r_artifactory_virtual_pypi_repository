# -*- mode: snippet -*-
# name: r_artifactory_virtual_pypi_repository
# key: r_artifactory_virtual_pypi_repository
# --

resource "artifactory_virtual_pypi_repository" "${1:this}" {
    key = "${2:key}"
}