# -*- mode: snippet -*-
# name: d_ultradns_rdpool
# key: d_ultradns_rdpool
# --

data "ultradns_rdpool" "${1:this}" {
    owner_name = "${2:owner_name}"
    record_type = "${3:record_type}"
    zone_name = "${4:zone_name}"
}