# -*- mode: snippet -*-
# name: d_ultradns_sfpool
# key: d_ultradns_sfpool
# --

data "ultradns_sfpool" "${1:this}" {
    owner_name = "${2:owner_name}"
    record_type = "${3:record_type}"
    zone_name = "${4:zone_name}"
}