# -*- mode: snippet -*-
# name: r_grafana_oncall_on_call_shift
# key: r_grafana_oncall_on_call_shift
# --

resource "grafana_oncall_on_call_shift" "${1:this}" {
    duration = "${2:duration}"
    name = "${3:name}"
    start = "${4:start}"
    type = "${5:type}"
}