# -*- mode: snippet -*-
# name: r_stackbill_volume_actions
# key: r_stackbill_volume_actions
# --

resource "stackbill_volume_actions" "${1:this}" {
    action = "${2:action}"
    instance_uuid = "${3:instance_uuid}"
    uuid = "${4:uuid}"
}