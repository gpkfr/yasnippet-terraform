# -*- mode: snippet -*-
# name: r_instaclustr_kafka_acl
# key: r_instaclustr_kafka_acl
# --

resource "instaclustr_kafka_acl" "${1:this}" {
    cluster_id = "${2:cluster_id}"
    host = "${3:host}"
    operation = "${4:operation}"
    pattern_type = "${5:pattern_type}"
    permission_type = "${6:permission_type}"
    principal = "${7:principal}"
    resource_name = "${8:resource_name}"
    resource_type = "${9:resource_type}"
}