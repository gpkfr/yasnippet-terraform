# -*- mode: snippet -*-
# name: r_instaclustr_kafka_user
# key: r_instaclustr_kafka_user
# --

resource "instaclustr_kafka_user" "${1:this}" {
    cluster_id = "${2:cluster_id}"
    password = "${3:password}"
    username = "${4:username}"
}