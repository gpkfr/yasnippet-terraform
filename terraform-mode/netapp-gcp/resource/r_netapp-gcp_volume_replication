# -*- mode: snippet -*-
# name: r_netapp-gcp_volume_replication
# key: r_netapp-gcp_volume_replication
# --

resource "netapp-gcp_volume_replication" "${1:this}" {
    name = "${2:name}"
    region = "${3:region}"
}