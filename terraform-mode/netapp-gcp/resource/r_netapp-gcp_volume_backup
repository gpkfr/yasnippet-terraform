# -*- mode: snippet -*-
# name: r_netapp-gcp_volume_backup
# key: r_netapp-gcp_volume_backup
# --

resource "netapp-gcp_volume_backup" "${1:this}" {
    name = "${2:name}"
    region = "${3:region}"
}