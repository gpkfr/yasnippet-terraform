# -*- mode: snippet -*-
# name: r_exoscale_database
# key: r_exoscale_database
# --

resource "exoscale_database" "${1:this}" {
    name = "${2:name}"
    plan = "${3:plan}"
    type = "${4:type}"
    zone = "${5:zone}"
}