# -*- mode: snippet -*-
# name: r_exoscale_sks_kubeconfig
# key: r_exoscale_sks_kubeconfig
# --

resource "exoscale_sks_kubeconfig" "${1:this}" {
    cluster_id = "${2:cluster_id}"
    groups = "${3:groups}"
    user = "${4:user}"
    zone = "${5:zone}"
}