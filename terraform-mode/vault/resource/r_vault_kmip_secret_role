# -*- mode: snippet -*-
# name: r_vault_kmip_secret_role
# key: r_vault_kmip_secret_role
# --

resource "vault_kmip_secret_role" "${1:this}" {
    path = "${2:path}"
    role = "${3:role}"
    scope = "${4:scope}"
}