# -*- mode: snippet -*-
# name: r_outscale_server_certificate
# key: r_outscale_server_certificate
# --

resource "outscale_server_certificate" "${1:this}" {
    body = "${2:body}"
    name = "${3:name}"
    private_key = "${4:private_key}"
}