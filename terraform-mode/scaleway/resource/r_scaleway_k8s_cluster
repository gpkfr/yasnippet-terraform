# -*- mode: snippet -*-
# name: r_scaleway_k8s_cluster
# key: r_scaleway_k8s_cluster
# --

resource "scaleway_k8s_cluster" "${1:this}" {
    cni = "${2:cni}"
    name = "${3:name}"
    version = "${4:version}"
}