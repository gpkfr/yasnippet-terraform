# -*- mode: snippet -*-
# name: r_scaleway_lb_backend
# key: r_scaleway_lb_backend
# --

resource "scaleway_lb_backend" "${1:this}" {
    forward_port = "${2:forward_port}"
    forward_protocol = "${3:forward_protocol}"
    lb_id = "${4:lb_id}"
}