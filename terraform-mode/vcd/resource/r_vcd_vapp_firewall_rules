# -*- mode: snippet -*-
# name: r_vcd_vapp_firewall_rules
# key: r_vcd_vapp_firewall_rules
# --

resource "vcd_vapp_firewall_rules" "${1:this}" {
    default_action = "${2:default_action}"
    network_id = "${3:network_id}"
    vapp_id = "${4:vapp_id}"
}