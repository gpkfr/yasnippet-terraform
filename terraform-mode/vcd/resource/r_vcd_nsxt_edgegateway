# -*- mode: snippet -*-
# name: r_vcd_nsxt_edgegateway
# key: r_vcd_nsxt_edgegateway
# --

resource "vcd_nsxt_edgegateway" "${1:this}" {
    external_network_id = "${2:external_network_id}"
    name = "${3:name}"
}