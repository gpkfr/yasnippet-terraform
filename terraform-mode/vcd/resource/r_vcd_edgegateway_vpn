# -*- mode: snippet -*-
# name: r_vcd_edgegateway_vpn
# key: r_vcd_edgegateway_vpn
# --

resource "vcd_edgegateway_vpn" "${1:this}" {
    edge_gateway = "${2:edge_gateway}"
    encryption_protocol = "${3:encryption_protocol}"
    local_id = "${4:local_id}"
    local_ip_address = "${5:local_ip_address}"
    mtu = "${6:mtu}"
    name = "${7:name}"
    peer_id = "${8:peer_id}"
    peer_ip_address = "${9:peer_ip_address}"
    shared_secret = "${10:shared_secret}"
}