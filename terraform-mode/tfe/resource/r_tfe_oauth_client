# -*- mode: snippet -*-
# name: r_tfe_oauth_client
# key: r_tfe_oauth_client
# --

resource "tfe_oauth_client" "${1:this}" {
    api_url = "${2:api_url}"
    http_url = "${3:http_url}"
    organization = "${4:organization}"
    service_provider = "${5:service_provider}"
}