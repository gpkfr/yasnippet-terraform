# -*- mode: snippet -*-
# name: r_kion_ou
# key: r_kion_ou
# --

resource "kion_ou" "${1:this}" {
    name = "${2:name}"
    parent_ou_id = "${3:parent_ou_id}"
    permission_scheme_id = "${4:permission_scheme_id}"
}