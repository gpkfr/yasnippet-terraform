# -*- mode: snippet -*-
# name: r_okta_app_oauth_api_scope
# key: r_okta_app_oauth_api_scope
# --

resource "okta_app_oauth_api_scope" "${1:this}" {
    app_id = "${2:app_id}"
    issuer = "${3:issuer}"
    scopes = "${4:scopes}"
}