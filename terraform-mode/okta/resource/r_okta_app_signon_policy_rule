# -*- mode: snippet -*-
# name: r_okta_app_signon_policy_rule
# key: r_okta_app_signon_policy_rule
# --

resource "okta_app_signon_policy_rule" "${1:this}" {
    name = "${2:name}"
    policy_id = "${3:policy_id}"
}