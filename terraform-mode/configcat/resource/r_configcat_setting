# -*- mode: snippet -*-
# name: r_configcat_setting
# key: r_configcat_setting
# --

resource "configcat_setting" "${1:this}" {
    config_id = "${2:config_id}"
    key = "${3:key}"
    name = "${4:name}"
}