# -*- mode: snippet -*-
# name: d_configcat_sdkkeys
# key: d_configcat_sdkkeys
# --

data "configcat_sdkkeys" "${1:this}" {
    config_id = "${2:config_id}"
    environment_id = "${3:environment_id}"
}