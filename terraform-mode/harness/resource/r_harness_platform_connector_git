# -*- mode: snippet -*-
# name: r_harness_platform_connector_git
# key: r_harness_platform_connector_git
# --

resource "harness_platform_connector_git" "${1:this}" {
    connection_type = "${2:connection_type}"
    identifier = "${3:identifier}"
    name = "${4:name}"
    url = "${5:url}"
}