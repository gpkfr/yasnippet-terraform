# -*- mode: snippet -*-
# name: r_harness_cloudprovider_azure
# key: r_harness_cloudprovider_azure
# --

resource "harness_cloudprovider_azure" "${1:this}" {
    client_id = "${2:client_id}"
    key = "${3:key}"
    name = "${4:name}"
    tenant_id = "${5:tenant_id}"
}