# -*- mode: snippet -*-
# name: r_harness_add_user_to_group
# key: r_harness_add_user_to_group
# --

resource "harness_add_user_to_group" "${1:this}" {
    group_id = "${2:group_id}"
    user_id = "${3:user_id}"
}