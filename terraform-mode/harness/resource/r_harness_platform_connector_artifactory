# -*- mode: snippet -*-
# name: r_harness_platform_connector_artifactory
# key: r_harness_platform_connector_artifactory
# --

resource "harness_platform_connector_artifactory" "${1:this}" {
    identifier = "${2:identifier}"
    name = "${3:name}"
    url = "${4:url}"
}