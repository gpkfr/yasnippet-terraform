# -*- mode: snippet -*-
# name: r_harness_environment
# key: r_harness_environment
# --

resource "harness_environment" "${1:this}" {
    app_id = "${2:app_id}"
    name = "${3:name}"
    type = "${4:type}"
}