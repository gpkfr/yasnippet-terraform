# -*- mode: snippet -*-
# name: r_harness_service_aws_lambda
# key: r_harness_service_aws_lambda
# --

resource "harness_service_aws_lambda" "${1:this}" {
    app_id = "${2:app_id}"
    name = "${3:name}"
}