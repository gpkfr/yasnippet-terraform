# -*- mode: snippet -*-
# name: r_harness_platform_project
# key: r_harness_platform_project
# --

resource "harness_platform_project" "${1:this}" {
    identifier = "${2:identifier}"
    name = "${3:name}"
    org_id = "${4:org_id}"
}