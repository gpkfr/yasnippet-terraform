# -*- mode: snippet -*-
# name: r_harness_service_kubernetes
# key: r_harness_service_kubernetes
# --

resource "harness_service_kubernetes" "${1:this}" {
    app_id = "${2:app_id}"
    name = "${3:name}"
}