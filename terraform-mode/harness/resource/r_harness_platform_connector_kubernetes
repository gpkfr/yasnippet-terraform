# -*- mode: snippet -*-
# name: r_harness_platform_connector_kubernetes
# key: r_harness_platform_connector_kubernetes
# --

resource "harness_platform_connector_kubernetes" "${1:this}" {
    identifier = "${2:identifier}"
    name = "${3:name}"
}