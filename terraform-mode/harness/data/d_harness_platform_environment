# -*- mode: snippet -*-
# name: d_harness_platform_environment
# key: d_harness_platform_environment
# --

data "harness_platform_environment" "${1:this}" {
    org_id = "${2:org_id}"
    project_id = "${3:project_id}"
}