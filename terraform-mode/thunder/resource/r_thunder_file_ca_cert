# -*- mode: snippet -*-
# name: r_thunder_file_ca_cert
# key: r_thunder_file_ca_cert
# --

resource "thunder_file_ca_cert" "${1:this}" {
    host = "${2:host}"
    name = "${3:name}"
    path = "${4:path}"
    protocol = "${5:protocol}"
}