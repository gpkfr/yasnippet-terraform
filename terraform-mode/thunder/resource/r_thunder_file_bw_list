# -*- mode: snippet -*-
# name: r_thunder_file_bw_list
# key: r_thunder_file_bw_list
# --

resource "thunder_file_bw_list" "${1:this}" {
    host = "${2:host}"
    name = "${3:name}"
    path = "${4:path}"
    protocol = "${5:protocol}"
}