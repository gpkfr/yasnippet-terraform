# -*- mode: snippet -*-
# name: d_google_service_networking_peered_dns_domain
# key: d_google_service_networking_peered_dns_domain
# --

data "google_service_networking_peered_dns_domain" "${1:this}" {
    name = "${2:name}"
    network = "${3:network}"
    project = "${4:project}"
    service = "${5:service}"
}