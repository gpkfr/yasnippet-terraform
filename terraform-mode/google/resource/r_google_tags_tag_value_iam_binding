# -*- mode: snippet -*-
# name: r_google_tags_tag_value_iam_binding
# key: r_google_tags_tag_value_iam_binding
# --

resource "google_tags_tag_value_iam_binding" "${1:this}" {
    members = "${2:members}"
    role = "${3:role}"
    tag_value = "${4:tag_value}"
}