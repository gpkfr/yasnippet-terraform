# -*- mode: snippet -*-
# name: r_google_privateca_certificate_authority
# key: r_google_privateca_certificate_authority
# --

resource "google_privateca_certificate_authority" "${1:this}" {
    certificate_authority_id = "${2:certificate_authority_id}"
    location = "${3:location}"
    pool = "${4:pool}"
}