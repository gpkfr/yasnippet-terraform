# -*- mode: snippet -*-
# name: r_google_tags_tag_value
# key: r_google_tags_tag_value
# --

resource "google_tags_tag_value" "${1:this}" {
    parent = "${2:parent}"
    short_name = "${3:short_name}"
}