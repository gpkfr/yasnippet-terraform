# -*- mode: snippet -*-
# name: r_anaml-operations_monitoring
# key: r_anaml-operations_monitoring
# --

resource "anaml-operations_monitoring" "${1:this}" {
    cluster = "${2:cluster}"
    enabled = "${3:enabled}"
    name = "${4:name}"
    tables = "${5:tables}"
}