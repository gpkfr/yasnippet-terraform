# -*- mode: snippet -*-
# name: r_anaml-operations_cluster
# key: r_anaml-operations_cluster
# --

resource "anaml-operations_cluster" "${1:this}" {
    description = "${2:description}"
    is_preview_cluster = "${3:is_preview_cluster}"
    name = "${4:name}"
}