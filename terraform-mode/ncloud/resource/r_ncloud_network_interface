# -*- mode: snippet -*-
# name: r_ncloud_network_interface
# key: r_ncloud_network_interface
# --

resource "ncloud_network_interface" "${1:this}" {
    access_control_groups = "${2:access_control_groups}"
    subnet_no = "${3:subnet_no}"
}