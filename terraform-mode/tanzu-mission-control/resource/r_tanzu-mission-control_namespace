# -*- mode: snippet -*-
# name: r_tanzu-mission-control_namespace
# key: r_tanzu-mission-control_namespace
# --

resource "tanzu-mission-control_namespace" "${1:this}" {
    cluster_name = "${2:cluster_name}"
    name = "${3:name}"
}