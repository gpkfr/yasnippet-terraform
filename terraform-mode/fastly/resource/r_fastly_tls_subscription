# -*- mode: snippet -*-
# name: r_fastly_tls_subscription
# key: r_fastly_tls_subscription
# --

resource "fastly_tls_subscription" "${1:this}" {
    certificate_authority = "${2:certificate_authority}"
    domains = "${3:domains}"
}