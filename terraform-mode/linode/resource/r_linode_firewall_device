# -*- mode: snippet -*-
# name: r_linode_firewall_device
# key: r_linode_firewall_device
# --

resource "linode_firewall_device" "${1:this}" {
    entity_id = "${2:entity_id}"
    firewall_id = "${3:firewall_id}"
}