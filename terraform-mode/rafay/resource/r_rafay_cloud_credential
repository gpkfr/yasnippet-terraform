# -*- mode: snippet -*-
# name: r_rafay_cloud_credential
# key: r_rafay_cloud_credential
# --

resource "rafay_cloud_credential" "${1:this}" {
    name = "${2:name}"
    project = "${3:project}"
    providertype = "${4:providertype}"
    type = "${5:type}"
}