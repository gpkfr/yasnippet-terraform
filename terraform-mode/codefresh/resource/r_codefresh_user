# -*- mode: snippet -*-
# name: r_codefresh_user
# key: r_codefresh_user
# --

resource "codefresh_user" "${1:this}" {
    accounts = "${2:accounts}"
    email = "${3:email}"
    user_name = "${4:user_name}"
}