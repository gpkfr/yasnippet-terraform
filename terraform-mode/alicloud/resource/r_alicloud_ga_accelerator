# -*- mode: snippet -*-
# name: r_alicloud_ga_accelerator
# key: r_alicloud_ga_accelerator
# --

resource "alicloud_ga_accelerator" "${1:this}" {
    duration = "${2:duration}"
    spec = "${3:spec}"
}