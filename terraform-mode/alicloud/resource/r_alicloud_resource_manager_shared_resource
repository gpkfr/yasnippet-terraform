# -*- mode: snippet -*-
# name: r_alicloud_resource_manager_shared_resource
# key: r_alicloud_resource_manager_shared_resource
# --

resource "alicloud_resource_manager_shared_resource" "${1:this}" {
    resource_id = "${2:resource_id}"
    resource_share_id = "${3:resource_share_id}"
    resource_type = "${4:resource_type}"
}