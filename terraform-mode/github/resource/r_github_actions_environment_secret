# -*- mode: snippet -*-
# name: r_github_actions_environment_secret
# key: r_github_actions_environment_secret
# --

resource "github_actions_environment_secret" "${1:this}" {
    environment = "${2:environment}"
    repository = "${3:repository}"
    secret_name = "${4:secret_name}"
}