# -*- mode: snippet -*-
# name: r_avi_wafcrs
# key: r_avi_wafcrs
# --

resource "avi_wafcrs" "${1:this}" {
    description = "${2:description}"
    integrity = "${3:integrity}"
    name = "${4:name}"
    release_date = "${5:release_date}"
    version = "${6:version}"
}