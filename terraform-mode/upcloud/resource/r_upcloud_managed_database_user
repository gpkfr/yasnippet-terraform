# -*- mode: snippet -*-
# name: r_upcloud_managed_database_user
# key: r_upcloud_managed_database_user
# --

resource "upcloud_managed_database_user" "${1:this}" {
    service = "${2:service}"
    username = "${3:username}"
}