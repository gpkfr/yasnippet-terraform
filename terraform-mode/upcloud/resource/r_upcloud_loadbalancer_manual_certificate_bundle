# -*- mode: snippet -*-
# name: r_upcloud_loadbalancer_manual_certificate_bundle
# key: r_upcloud_loadbalancer_manual_certificate_bundle
# --

resource "upcloud_loadbalancer_manual_certificate_bundle" "${1:this}" {
    certificate = "${2:certificate}"
    name = "${3:name}"
    private_key = "${4:private_key}"
}