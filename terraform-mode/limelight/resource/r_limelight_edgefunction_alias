# -*- mode: snippet -*-
# name: r_limelight_edgefunction_alias
# key: r_limelight_edgefunction_alias
# --

resource "limelight_edgefunction_alias" "${1:this}" {
    function_name = "${2:function_name}"
    function_version = "${3:function_version}"
    name = "${4:name}"
    shortname = "${5:shortname}"
}