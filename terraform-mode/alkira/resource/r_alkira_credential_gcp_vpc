# -*- mode: snippet -*-
# name: r_alkira_credential_gcp_vpc
# key: r_alkira_credential_gcp_vpc
# --

resource "alkira_credential_gcp_vpc" "${1:this}" {
    client_email = "${2:client_email}"
    client_id = "${3:client_id}"
    client_x509_cert_url = "${4:client_x509_cert_url}"
    name = "${5:name}"
    private_key = "${6:private_key}"
    private_key_id = "${7:private_key_id}"
    project_id = "${8:project_id}"
}