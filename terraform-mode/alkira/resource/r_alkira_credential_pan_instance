# -*- mode: snippet -*-
# name: r_alkira_credential_pan_instance
# key: r_alkira_credential_pan_instance
# --

resource "alkira_credential_pan_instance" "${1:this}" {
    auth_code = "${2:auth_code}"
    auth_key = "${3:auth_key}"
    license_key = "${4:license_key}"
    name = "${5:name}"
    password = "${6:password}"
    username = "${7:username}"
}