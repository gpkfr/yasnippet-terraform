# -*- mode: snippet -*-
# name: r_alkira_credential_azure_vnet
# key: r_alkira_credential_azure_vnet
# --

resource "alkira_credential_azure_vnet" "${1:this}" {
    application_id = "${2:application_id}"
    name = "${3:name}"
    secret_key = "${4:secret_key}"
    tenant_id = "${5:tenant_id}"
}