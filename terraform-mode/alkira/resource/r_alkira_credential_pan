# -*- mode: snippet -*-
# name: r_alkira_credential_pan
# key: r_alkira_credential_pan
# --

resource "alkira_credential_pan" "${1:this}" {
    license_key = "${2:license_key}"
    name = "${3:name}"
    password = "${4:password}"
    username = "${5:username}"
}