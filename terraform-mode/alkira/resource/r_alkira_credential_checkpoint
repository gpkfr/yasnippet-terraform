# -*- mode: snippet -*-
# name: r_alkira_credential_checkpoint
# key: r_alkira_credential_checkpoint
# --

resource "alkira_credential_checkpoint" "${1:this}" {
    name = "${2:name}"
    password = "${3:password}"
    sic_keys = "${4:sic_keys}"
}