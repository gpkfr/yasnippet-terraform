# -*- mode: snippet -*-
# name: r_alkira_credential_oci_vcn
# key: r_alkira_credential_oci_vcn
# --

resource "alkira_credential_oci_vcn" "${1:this}" {
    fingerprint = "${2:fingerprint}"
    key = "${3:key}"
    name = "${4:name}"
    tenant_ocid = "${5:tenant_ocid}"
    user_ocid = "${6:user_ocid}"
}