# -*- mode: snippet -*-
# name: r_alkira_connector_ipsec
# key: r_alkira_connector_ipsec
# --

resource "alkira_connector_ipsec" "${1:this}" {
    cxp = "${2:cxp}"
    name = "${3:name}"
    segment_id = "${4:segment_id}"
    size = "${5:size}"
    vpn_mode = "${6:vpn_mode}"
}