# -*- mode: snippet -*-
# name: r_alkira_credential_fortinet_instance
# key: r_alkira_credential_fortinet_instance
# --

resource "alkira_credential_fortinet_instance" "${1:this}" {
    license_key = "${2:license_key}"
    license_type = "${3:license_type}"
    name = "${4:name}"
}