# -*- mode: snippet -*-
# name: r_oci_identity_db_credential
# key: r_oci_identity_db_credential
# --

resource "oci_identity_db_credential" "${1:this}" {
    description = "${2:description}"
    password = "${3:password}"
    user_id = "${4:user_id}"
}