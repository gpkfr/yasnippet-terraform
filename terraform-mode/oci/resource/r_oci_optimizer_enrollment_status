# -*- mode: snippet -*-
# name: r_oci_optimizer_enrollment_status
# key: r_oci_optimizer_enrollment_status
# --

resource "oci_optimizer_enrollment_status" "${1:this}" {
    enrollment_status_id = "${2:enrollment_status_id}"
    status = "${3:status}"
}