# -*- mode: snippet -*-
# name: r_oci_email_email_domain
# key: r_oci_email_email_domain
# --

resource "oci_email_email_domain" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    name = "${3:name}"
}