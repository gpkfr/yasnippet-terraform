# -*- mode: snippet -*-
# name: r_oci_vault_secret
# key: r_oci_vault_secret
# --

resource "oci_vault_secret" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    key_id = "${3:key_id}"
    secret_name = "${4:secret_name}"
    vault_id = "${5:vault_id}"
}