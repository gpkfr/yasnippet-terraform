# -*- mode: snippet -*-
# name: r_oci_core_vtap
# key: r_oci_core_vtap
# --

resource "oci_core_vtap" "${1:this}" {
    capture_filter_id = "${2:capture_filter_id}"
    compartment_id = "${3:compartment_id}"
    source_id = "${4:source_id}"
    vcn_id = "${5:vcn_id}"
}