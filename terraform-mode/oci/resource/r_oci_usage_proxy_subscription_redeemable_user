# -*- mode: snippet -*-
# name: r_oci_usage_proxy_subscription_redeemable_user
# key: r_oci_usage_proxy_subscription_redeemable_user
# --

resource "oci_usage_proxy_subscription_redeemable_user" "${1:this}" {
    subscription_id = "${2:subscription_id}"
    tenancy_id = "${3:tenancy_id}"
}