# -*- mode: snippet -*-
# name: d_oci_core_block_volume_replicas
# key: d_oci_core_block_volume_replicas
# --

data "oci_core_block_volume_replicas" "${1:this}" {
    availability_domain = "${2:availability_domain}"
    compartment_id = "${3:compartment_id}"
}