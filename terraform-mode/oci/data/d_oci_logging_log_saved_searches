# -*- mode: snippet -*-
# name: d_oci_logging_log_saved_searches
# key: d_oci_logging_log_saved_searches
# --

data "oci_logging_log_saved_searches" "${1:this}" {
    compartment_id = "${2:compartment_id}"
}