# -*- mode: snippet -*-
# name: d_oci_usage_proxy_subscription_product
# key: d_oci_usage_proxy_subscription_product
# --

data "oci_usage_proxy_subscription_product" "${1:this}" {
    subscription_id = "${2:subscription_id}"
    tenancy_id = "${3:tenancy_id}"
    usage_period_key = "${4:usage_period_key}"
}