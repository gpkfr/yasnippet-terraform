# -*- mode: snippet -*-
# name: d_oci_usage_proxy_subscription_redeemable_user
# key: d_oci_usage_proxy_subscription_redeemable_user
# --

data "oci_usage_proxy_subscription_redeemable_user" "${1:this}" {
    subscription_id = "${2:subscription_id}"
    tenancy_id = "${3:tenancy_id}"
}