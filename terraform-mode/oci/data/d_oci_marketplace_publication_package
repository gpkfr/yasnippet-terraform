# -*- mode: snippet -*-
# name: d_oci_marketplace_publication_package
# key: d_oci_marketplace_publication_package
# --

data "oci_marketplace_publication_package" "${1:this}" {
    package_version = "${2:package_version}"
    publication_id = "${3:publication_id}"
}