# -*- mode: snippet -*-
# name: d_oci_computeinstanceagent_instance_available_plugins
# key: d_oci_computeinstanceagent_instance_available_plugins
# --

data "oci_computeinstanceagent_instance_available_plugins" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    os_name = "${3:os_name}"
    os_version = "${4:os_version}"
}