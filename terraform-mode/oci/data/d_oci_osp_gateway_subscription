# -*- mode: snippet -*-
# name: d_oci_osp_gateway_subscription
# key: d_oci_osp_gateway_subscription
# --

data "oci_osp_gateway_subscription" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    osp_home_region = "${3:osp_home_region}"
    subscription_id = "${4:subscription_id}"
}