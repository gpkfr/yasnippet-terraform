# -*- mode: snippet -*-
# name: d_volterra_address_allocator
# key: d_volterra_address_allocator
# --

data "volterra_address_allocator" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
}