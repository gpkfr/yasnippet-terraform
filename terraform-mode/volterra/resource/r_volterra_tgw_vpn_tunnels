# -*- mode: snippet -*-
# name: r_volterra_tgw_vpn_tunnels
# key: r_volterra_tgw_vpn_tunnels
# --

resource "volterra_tgw_vpn_tunnels" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
}