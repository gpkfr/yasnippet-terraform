# -*- mode: snippet -*-
# name: r_volterra_aws_vpc_site
# key: r_volterra_aws_vpc_site
# --

resource "volterra_aws_vpc_site" "${1:this}" {
    aws_region = "${2:aws_region}"
    instance_type = "${3:instance_type}"
    name = "${4:name}"
    namespace = "${5:namespace}"
}