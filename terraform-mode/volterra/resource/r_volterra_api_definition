# -*- mode: snippet -*-
# name: r_volterra_api_definition
# key: r_volterra_api_definition
# --

resource "volterra_api_definition" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
    swagger_specs = "${4:swagger_specs}"
}