# -*- mode: snippet -*-
# name: r_volterra_tcp_loadbalancer
# key: r_volterra_tcp_loadbalancer
# --

resource "volterra_tcp_loadbalancer" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
}