# -*- mode: snippet -*-
# name: r_volterra_cloud_credentials
# key: r_volterra_cloud_credentials
# --

resource "volterra_cloud_credentials" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
}