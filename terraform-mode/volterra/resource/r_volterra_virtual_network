# -*- mode: snippet -*-
# name: r_volterra_virtual_network
# key: r_volterra_virtual_network
# --

resource "volterra_virtual_network" "${1:this}" {
    name = "${2:name}"
    namespace = "${3:namespace}"
}