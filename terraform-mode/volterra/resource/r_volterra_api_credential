# -*- mode: snippet -*-
# name: r_volterra_api_credential
# key: r_volterra_api_credential
# --

resource "volterra_api_credential" "${1:this}" {
    api_credential_type = "${2:api_credential_type}"
    name = "${3:name}"
}