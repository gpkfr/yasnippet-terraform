# -*- mode: snippet -*-
# name: d_firehydrant_runbook_action
# key: d_firehydrant_runbook_action
# --

data "firehydrant_runbook_action" "${1:this}" {
    integration_slug = "${2:integration_slug}"
    slug = "${3:slug}"
    type = "${4:type}"
}