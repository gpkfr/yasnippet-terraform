# -*- mode: snippet -*-
# name: r_google_api_gateway_gateway_iam_binding
# key: r_google_api_gateway_gateway_iam_binding
# --

resource "google_api_gateway_gateway_iam_binding" "${1:this}" {
    gateway = "${2:gateway}"
    members = "${3:members}"
    role = "${4:role}"
}