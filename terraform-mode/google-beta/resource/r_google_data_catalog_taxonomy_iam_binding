# -*- mode: snippet -*-
# name: r_google_data_catalog_taxonomy_iam_binding
# key: r_google_data_catalog_taxonomy_iam_binding
# --

resource "google_data_catalog_taxonomy_iam_binding" "${1:this}" {
    members = "${2:members}"
    role = "${3:role}"
    taxonomy = "${4:taxonomy}"
}