# -*- mode: snippet -*-
# name: r_google_container_aws_cluster
# key: r_google_container_aws_cluster
# --

resource "google_container_aws_cluster" "${1:this}" {
    aws_region = "${2:aws_region}"
    location = "${3:location}"
    name = "${4:name}"
}