# -*- mode: snippet -*-
# name: r_google_api_gateway_api_iam_binding
# key: r_google_api_gateway_api_iam_binding
# --

resource "google_api_gateway_api_iam_binding" "${1:this}" {
    api = "${2:api}"
    members = "${3:members}"
    role = "${4:role}"
}