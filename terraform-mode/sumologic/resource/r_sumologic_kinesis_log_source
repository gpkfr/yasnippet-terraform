# -*- mode: snippet -*-
# name: r_sumologic_kinesis_log_source
# key: r_sumologic_kinesis_log_source
# --

resource "sumologic_kinesis_log_source" "${1:this}" {
    collector_id = "${2:collector_id}"
    content_type = "${3:content_type}"
    name = "${4:name}"
}