# -*- mode: snippet -*-
# name: r_nsxt_policy_ospf_config
# key: r_nsxt_policy_ospf_config
# --

resource "nsxt_policy_ospf_config" "${1:this}" {
    display_name = "${2:display_name}"
    gateway_path = "${3:gateway_path}"
}