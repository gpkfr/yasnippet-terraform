# -*- mode: snippet -*-
# name: d_azurerm_site_recovery_fabric
# key: d_azurerm_site_recovery_fabric
# --

data "azurerm_site_recovery_fabric" "${1:this}" {
    name = "${2:name}"
    recovery_vault_name = "${3:recovery_vault_name}"
    resource_group_name = "${4:resource_group_name}"
}