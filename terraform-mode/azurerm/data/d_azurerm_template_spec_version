# -*- mode: snippet -*-
# name: d_azurerm_template_spec_version
# key: d_azurerm_template_spec_version
# --

data "azurerm_template_spec_version" "${1:this}" {
    name = "${2:name}"
    resource_group_name = "${3:resource_group_name}"
    version = "${4:version}"
}