# -*- mode: snippet -*-
# name: d_azurerm_site_recovery_replication_policy
# key: d_azurerm_site_recovery_replication_policy
# --

data "azurerm_site_recovery_replication_policy" "${1:this}" {
    name = "${2:name}"
    recovery_vault_name = "${3:recovery_vault_name}"
    resource_group_name = "${4:resource_group_name}"
}