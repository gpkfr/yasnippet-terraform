# -*- mode: snippet -*-
# name: r_azurerm_media_live_event_output
# key: r_azurerm_media_live_event_output
# --

resource "azurerm_media_live_event_output" "${1:this}" {
    archive_window_duration = "${2:archive_window_duration}"
    asset_name = "${3:asset_name}"
    live_event_id = "${4:live_event_id}"
    name = "${5:name}"
}