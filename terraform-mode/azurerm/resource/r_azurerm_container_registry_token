# -*- mode: snippet -*-
# name: r_azurerm_container_registry_token
# key: r_azurerm_container_registry_token
# --

resource "azurerm_container_registry_token" "${1:this}" {
    container_registry_name = "${2:container_registry_name}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    scope_map_id = "${5:scope_map_id}"
}