# -*- mode: snippet -*-
# name: r_azurerm_mssql_job_credential
# key: r_azurerm_mssql_job_credential
# --

resource "azurerm_mssql_job_credential" "${1:this}" {
    job_agent_id = "${2:job_agent_id}"
    name = "${3:name}"
    password = "${4:password}"
    username = "${5:username}"
}