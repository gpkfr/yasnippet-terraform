# -*- mode: snippet -*-
# name: r_azurerm_virtual_hub_ip
# key: r_azurerm_virtual_hub_ip
# --

resource "azurerm_virtual_hub_ip" "${1:this}" {
    name = "${2:name}"
    public_ip_address_id = "${3:public_ip_address_id}"
    subnet_id = "${4:subnet_id}"
    virtual_hub_id = "${5:virtual_hub_id}"
}