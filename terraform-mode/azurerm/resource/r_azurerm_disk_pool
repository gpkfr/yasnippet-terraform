# -*- mode: snippet -*-
# name: r_azurerm_disk_pool
# key: r_azurerm_disk_pool
# --

resource "azurerm_disk_pool" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
    subnet_id = "${6:subnet_id}"
    zones = "${7:zones}"
}