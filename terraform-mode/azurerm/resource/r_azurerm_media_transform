# -*- mode: snippet -*-
# name: r_azurerm_media_transform
# key: r_azurerm_media_transform
# --

resource "azurerm_media_transform" "${1:this}" {
    media_services_account_name = "${2:media_services_account_name}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
}