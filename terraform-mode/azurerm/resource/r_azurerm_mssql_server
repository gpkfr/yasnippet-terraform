# -*- mode: snippet -*-
# name: r_azurerm_mssql_server
# key: r_azurerm_mssql_server
# --

resource "azurerm_mssql_server" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    version = "${5:version}"
}