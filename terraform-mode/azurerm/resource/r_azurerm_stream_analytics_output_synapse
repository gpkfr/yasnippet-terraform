# -*- mode: snippet -*-
# name: r_azurerm_stream_analytics_output_synapse
# key: r_azurerm_stream_analytics_output_synapse
# --

resource "azurerm_stream_analytics_output_synapse" "${1:this}" {
    database = "${2:database}"
    name = "${3:name}"
    password = "${4:password}"
    resource_group_name = "${5:resource_group_name}"
    server = "${6:server}"
    stream_analytics_job_name = "${7:stream_analytics_job_name}"
    table = "${8:table}"
    user = "${9:user}"
}