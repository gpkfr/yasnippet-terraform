# -*- mode: snippet -*-
# name: r_azurerm_sql_managed_instance_active_directory_administrator
# key: r_azurerm_sql_managed_instance_active_directory_administrator
# --

resource "azurerm_sql_managed_instance_active_directory_administrator" "${1:this}" {
    login = "${2:login}"
    managed_instance_name = "${3:managed_instance_name}"
    object_id = "${4:object_id}"
    resource_group_name = "${5:resource_group_name}"
    tenant_id = "${6:tenant_id}"
}