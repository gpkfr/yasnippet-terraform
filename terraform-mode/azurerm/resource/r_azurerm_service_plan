# -*- mode: snippet -*-
# name: r_azurerm_service_plan
# key: r_azurerm_service_plan
# --

resource "azurerm_service_plan" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    os_type = "${4:os_type}"
    resource_group_name = "${5:resource_group_name}"
    sku_name = "${6:sku_name}"
}