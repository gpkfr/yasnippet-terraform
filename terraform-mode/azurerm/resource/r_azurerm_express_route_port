# -*- mode: snippet -*-
# name: r_azurerm_express_route_port
# key: r_azurerm_express_route_port
# --

resource "azurerm_express_route_port" "${1:this}" {
    bandwidth_in_gbps = "${2:bandwidth_in_gbps}"
    encapsulation = "${3:encapsulation}"
    location = "${4:location}"
    name = "${5:name}"
    peering_location = "${6:peering_location}"
    resource_group_name = "${7:resource_group_name}"
}