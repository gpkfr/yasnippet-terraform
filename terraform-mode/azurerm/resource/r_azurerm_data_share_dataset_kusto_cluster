# -*- mode: snippet -*-
# name: r_azurerm_data_share_dataset_kusto_cluster
# key: r_azurerm_data_share_dataset_kusto_cluster
# --

resource "azurerm_data_share_dataset_kusto_cluster" "${1:this}" {
    kusto_cluster_id = "${2:kusto_cluster_id}"
    name = "${3:name}"
    share_id = "${4:share_id}"
}