# -*- mode: snippet -*-
# name: r_databricks_git_credential
# key: r_databricks_git_credential
# --

resource "databricks_git_credential" "${1:this}" {
    git_provider = "${2:git_provider}"
    git_username = "${3:git_username}"
    personal_access_token = "${4:personal_access_token}"
}