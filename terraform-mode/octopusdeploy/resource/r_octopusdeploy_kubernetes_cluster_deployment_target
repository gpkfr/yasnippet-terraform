# -*- mode: snippet -*-
# name: r_octopusdeploy_kubernetes_cluster_deployment_target
# key: r_octopusdeploy_kubernetes_cluster_deployment_target
# --

resource "octopusdeploy_kubernetes_cluster_deployment_target" "${1:this}" {
    cluster_url = "${2:cluster_url}"
    environments = "${3:environments}"
    name = "${4:name}"
    roles = "${5:roles}"
}