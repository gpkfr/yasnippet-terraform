# -*- mode: snippet -*-
# name: r_octopusdeploy_offline_package_drop_deployment_target
# key: r_octopusdeploy_offline_package_drop_deployment_target
# --

resource "octopusdeploy_offline_package_drop_deployment_target" "${1:this}" {
    applications_directory = "${2:applications_directory}"
    environments = "${3:environments}"
    name = "${4:name}"
    roles = "${5:roles}"
    working_directory = "${6:working_directory}"
}