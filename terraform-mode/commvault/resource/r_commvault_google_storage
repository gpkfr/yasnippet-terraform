# -*- mode: snippet -*-
# name: r_commvault_google_storage
# key: r_commvault_google_storage
# --

resource "commvault_google_storage" "${1:this}" {
    bucket = "${2:bucket}"
    mediaagent = "${3:mediaagent}"
    service_host = "${4:service_host}"
    storage_name = "${5:storage_name}"
}