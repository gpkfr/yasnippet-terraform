# -*- mode: snippet -*-
# name: r_commvault_user
# key: r_commvault_user
# --

resource "commvault_user" "${1:this}" {
    email = "${2:email}"
    password = "${3:password}"
    user_name = "${4:user_name}"
}