# -*- mode: snippet -*-
# name: r_commvault_install_ma
# key: r_commvault_install_ma
# --

resource "commvault_install_ma" "${1:this}" {
    hostname = "${2:hostname}"
    mediaagent_name = "${3:mediaagent_name}"
    password = "${4:password}"
    user_name = "${5:user_name}"
}