# -*- mode: snippet -*-
# name: r_rockset_dynamodb_collection
# key: r_rockset_dynamodb_collection
# --

resource "rockset_dynamodb_collection" "${1:this}" {
    name = "${2:name}"
    workspace = "${3:workspace}"
}