# -*- mode: snippet -*-
# name: r_rockset_mongodb_collection
# key: r_rockset_mongodb_collection
# --

resource "rockset_mongodb_collection" "${1:this}" {
    name = "${2:name}"
    workspace = "${3:workspace}"
}