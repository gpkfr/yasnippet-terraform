# -*- mode: snippet -*-
# name: r_rockset_s3_integration
# key: r_rockset_s3_integration
# --

resource "rockset_s3_integration" "${1:this}" {
    aws_role_arn = "${2:aws_role_arn}"
    name = "${3:name}"
}