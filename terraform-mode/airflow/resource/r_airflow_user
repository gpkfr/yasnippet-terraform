# -*- mode: snippet -*-
# name: r_airflow_user
# key: r_airflow_user
# --

resource "airflow_user" "${1:this}" {
    email = "${2:email}"
    first_name = "${3:first_name}"
    last_name = "${4:last_name}"
    username = "${5:username}"
}