# -*- mode: snippet -*-
# name: r_mso_schema_site_external_epg
# key: r_mso_schema_site_external_epg
# --

resource "mso_schema_site_external_epg" "${1:this}" {
    external_epg_name = "${2:external_epg_name}"
    schema_id = "${3:schema_id}"
    site_id = "${4:site_id}"
    template_name = "${5:template_name}"
}