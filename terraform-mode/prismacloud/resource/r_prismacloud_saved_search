# -*- mode: snippet -*-
# name: r_prismacloud_saved_search
# key: r_prismacloud_saved_search
# --

resource "prismacloud_saved_search" "${1:this}" {
    name = "${2:name}"
    query = "${3:query}"
    search_id = "${4:search_id}"
}