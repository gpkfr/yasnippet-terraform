# -*- mode: snippet -*-
# name: r_ec_deployment_elasticsearch_keystore
# key: r_ec_deployment_elasticsearch_keystore
# --

resource "ec_deployment_elasticsearch_keystore" "${1:this}" {
    deployment_id = "${2:deployment_id}"
    setting_name = "${3:setting_name}"
    value = "${4:value}"
}