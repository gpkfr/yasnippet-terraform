# -*- mode: snippet -*-
# name: d_aws_eks_addon_version
# key: d_aws_eks_addon_version
# --

data "aws_eks_addon_version" "${1:this}" {
    addon_name = "${2:addon_name}"
    kubernetes_version = "${3:kubernetes_version}"
}