# -*- mode: snippet -*-
# name: d_aws_eks_node_group
# key: d_aws_eks_node_group
# --

data "aws_eks_node_group" "${1:this}" {
    cluster_name = "${2:cluster_name}"
    node_group_name = "${3:node_group_name}"
}