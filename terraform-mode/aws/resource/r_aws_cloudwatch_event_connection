# -*- mode: snippet -*-
# name: r_aws_cloudwatch_event_connection
# key: r_aws_cloudwatch_event_connection
# --

resource "aws_cloudwatch_event_connection" "${1:this}" {
    authorization_type = "${2:authorization_type}"
    name = "${3:name}"
}