# -*- mode: snippet -*-
# name: r_aws_iot_provisioning_template
# key: r_aws_iot_provisioning_template
# --

resource "aws_iot_provisioning_template" "${1:this}" {
    name = "${2:name}"
    provisioning_role_arn = "${3:provisioning_role_arn}"
    template_body = "${4:template_body}"
}