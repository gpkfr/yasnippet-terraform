# -*- mode: snippet -*-
# name: r_aws_msk_scram_secret_association
# key: r_aws_msk_scram_secret_association
# --

resource "aws_msk_scram_secret_association" "${1:this}" {
    cluster_arn = "${2:cluster_arn}"
    secret_arn_list = "${3:secret_arn_list}"
}