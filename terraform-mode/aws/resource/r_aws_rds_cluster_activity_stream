# -*- mode: snippet -*-
# name: r_aws_rds_cluster_activity_stream
# key: r_aws_rds_cluster_activity_stream
# --

resource "aws_rds_cluster_activity_stream" "${1:this}" {
    kms_key_id = "${2:kms_key_id}"
    mode = "${3:mode}"
    resource_arn = "${4:resource_arn}"
}