# -*- mode: snippet -*-
# name: r_aws_storagegateway_tape_pool
# key: r_aws_storagegateway_tape_pool
# --

resource "aws_storagegateway_tape_pool" "${1:this}" {
    pool_name = "${2:pool_name}"
    storage_class = "${3:storage_class}"
}