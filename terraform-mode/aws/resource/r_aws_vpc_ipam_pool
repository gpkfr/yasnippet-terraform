# -*- mode: snippet -*-
# name: r_aws_vpc_ipam_pool
# key: r_aws_vpc_ipam_pool
# --

resource "aws_vpc_ipam_pool" "${1:this}" {
    address_family = "${2:address_family}"
    ipam_scope_id = "${3:ipam_scope_id}"
}