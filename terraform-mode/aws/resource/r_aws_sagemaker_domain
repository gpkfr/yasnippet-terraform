# -*- mode: snippet -*-
# name: r_aws_sagemaker_domain
# key: r_aws_sagemaker_domain
# --

resource "aws_sagemaker_domain" "${1:this}" {
    auth_mode = "${2:auth_mode}"
    domain_name = "${3:domain_name}"
    subnet_ids = "${4:subnet_ids}"
    vpc_id = "${5:vpc_id}"
}