# -*- mode: snippet -*-
# name: r_aws_lambda_layer_version_permission
# key: r_aws_lambda_layer_version_permission
# --

resource "aws_lambda_layer_version_permission" "${1:this}" {
    action = "${2:action}"
    layer_name = "${3:layer_name}"
    principal = "${4:principal}"
    statement_id = "${5:statement_id}"
    version_number = "${6:version_number}"
}