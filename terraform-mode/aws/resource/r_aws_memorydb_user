# -*- mode: snippet -*-
# name: r_aws_memorydb_user
# key: r_aws_memorydb_user
# --

resource "aws_memorydb_user" "${1:this}" {
    access_string = "${2:access_string}"
    user_name = "${3:user_name}"
}