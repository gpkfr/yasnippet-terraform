# -*- mode: snippet -*-
# name: r_aws_db_proxy_endpoint
# key: r_aws_db_proxy_endpoint
# --

resource "aws_db_proxy_endpoint" "${1:this}" {
    db_proxy_endpoint_name = "${2:db_proxy_endpoint_name}"
    db_proxy_name = "${3:db_proxy_name}"
    vpc_subnet_ids = "${4:vpc_subnet_ids}"
}