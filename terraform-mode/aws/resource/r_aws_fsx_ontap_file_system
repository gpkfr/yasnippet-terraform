# -*- mode: snippet -*-
# name: r_aws_fsx_ontap_file_system
# key: r_aws_fsx_ontap_file_system
# --

resource "aws_fsx_ontap_file_system" "${1:this}" {
    deployment_type = "${2:deployment_type}"
    preferred_subnet_id = "${3:preferred_subnet_id}"
    subnet_ids = "${4:subnet_ids}"
    throughput_capacity = "${5:throughput_capacity}"
}