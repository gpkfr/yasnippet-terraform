# -*- mode: snippet -*-
# name: r_aws_connect_contact_flow
# key: r_aws_connect_contact_flow
# --

resource "aws_connect_contact_flow" "${1:this}" {
    instance_id = "${2:instance_id}"
    name = "${3:name}"
}