# -*- mode: snippet -*-
# name: r_aws_secretsmanager_secret_policy
# key: r_aws_secretsmanager_secret_policy
# --

resource "aws_secretsmanager_secret_policy" "${1:this}" {
    policy = "${2:policy}"
    secret_arn = "${3:secret_arn}"
}