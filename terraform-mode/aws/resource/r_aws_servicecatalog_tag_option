# -*- mode: snippet -*-
# name: r_aws_servicecatalog_tag_option
# key: r_aws_servicecatalog_tag_option
# --

resource "aws_servicecatalog_tag_option" "${1:this}" {
    key = "${2:key}"
    value = "${3:value}"
}