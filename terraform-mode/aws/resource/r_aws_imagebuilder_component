# -*- mode: snippet -*-
# name: r_aws_imagebuilder_component
# key: r_aws_imagebuilder_component
# --

resource "aws_imagebuilder_component" "${1:this}" {
    name = "${2:name}"
    platform = "${3:platform}"
    version = "${4:version}"
}