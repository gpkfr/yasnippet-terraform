# -*- mode: snippet -*-
# name: r_aws_service_discovery_instance
# key: r_aws_service_discovery_instance
# --

resource "aws_service_discovery_instance" "${1:this}" {
    attributes = "${2:attributes}"
    instance_id = "${3:instance_id}"
    service_id = "${4:service_id}"
}