# -*- mode: snippet -*-
# name: r_aws_codecommit_approval_rule_template
# key: r_aws_codecommit_approval_rule_template
# --

resource "aws_codecommit_approval_rule_template" "${1:this}" {
    content = "${2:content}"
    name = "${3:name}"
}