# -*- mode: snippet -*-
# name: r_aws_appsync_resolver
# key: r_aws_appsync_resolver
# --

resource "aws_appsync_resolver" "${1:this}" {
    api_id = "${2:api_id}"
    field = "${3:field}"
    type = "${4:type}"
}