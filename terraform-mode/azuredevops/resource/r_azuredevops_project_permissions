# -*- mode: snippet -*-
# name: r_azuredevops_project_permissions
# key: r_azuredevops_project_permissions
# --

resource "azuredevops_project_permissions" "${1:this}" {
    permissions = "${2:permissions}"
    principal = "${3:principal}"
    project_id = "${4:project_id}"
}