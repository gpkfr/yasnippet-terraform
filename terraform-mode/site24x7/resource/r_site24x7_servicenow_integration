# -*- mode: snippet -*-
# name: r_site24x7_servicenow_integration
# key: r_site24x7_servicenow_integration
# --

resource "site24x7_servicenow_integration" "${1:this}" {
    instance_url = "${2:instance_url}"
    name = "${3:name}"
    password = "${4:password}"
    sender_name = "${5:sender_name}"
    title = "${6:title}"
    user_name = "${7:user_name}"
}