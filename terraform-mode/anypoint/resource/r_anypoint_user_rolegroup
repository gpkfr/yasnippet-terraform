# -*- mode: snippet -*-
# name: r_anypoint_user_rolegroup
# key: r_anypoint_user_rolegroup
# --

resource "anypoint_user_rolegroup" "${1:this}" {
    org_id = "${2:org_id}"
    rolegroup_id = "${3:rolegroup_id}"
    user_id = "${4:user_id}"
}