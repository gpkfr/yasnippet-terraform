# -*- mode: snippet -*-
# name: r_anypoint_vpc
# key: r_anypoint_vpc
# --

resource "anypoint_vpc" "${1:this}" {
    cidr_block = "${2:cidr_block}"
    name = "${3:name}"
    org_id = "${4:org_id}"
    region = "${5:region}"
}