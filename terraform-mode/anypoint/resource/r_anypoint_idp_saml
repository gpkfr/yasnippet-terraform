# -*- mode: snippet -*-
# name: r_anypoint_idp_saml
# key: r_anypoint_idp_saml
# --

resource "anypoint_idp_saml" "${1:this}" {
    name = "${2:name}"
    org_id = "${3:org_id}"
    sp_sign_on_url = "${4:sp_sign_on_url}"
    sp_sign_out_url = "${5:sp_sign_out_url}"
}