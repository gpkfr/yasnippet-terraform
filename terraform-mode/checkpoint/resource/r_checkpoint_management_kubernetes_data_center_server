# -*- mode: snippet -*-
# name: r_checkpoint_management_kubernetes_data_center_server
# key: r_checkpoint_management_kubernetes_data_center_server
# --

resource "checkpoint_management_kubernetes_data_center_server" "${1:this}" {
    hostname = "${2:hostname}"
    name = "${3:name}"
    token_file = "${4:token_file}"
}