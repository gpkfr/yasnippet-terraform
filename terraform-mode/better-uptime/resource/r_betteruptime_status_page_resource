# -*- mode: snippet -*-
# name: r_betteruptime_status_page_resource
# key: r_betteruptime_status_page_resource
# --

resource "betteruptime_status_page_resource" "${1:this}" {
    public_name = "${2:public_name}"
    resource_id = "${3:resource_id}"
    resource_type = "${4:resource_type}"
    status_page_id = "${5:status_page_id}"
}