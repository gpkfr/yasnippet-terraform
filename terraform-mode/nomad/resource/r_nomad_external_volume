# -*- mode: snippet -*-
# name: r_nomad_external_volume
# key: r_nomad_external_volume
# --

resource "nomad_external_volume" "${1:this}" {
    name = "${2:name}"
    plugin_id = "${3:plugin_id}"
    volume_id = "${4:volume_id}"
}