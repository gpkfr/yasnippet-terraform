# -*- mode: snippet -*-
# name: r_dynatrace_cloudfoundry_credentials
# key: r_dynatrace_cloudfoundry_credentials
# --

resource "dynatrace_cloudfoundry_credentials" "${1:this}" {
    api_url = "${2:api_url}"
    login_url = "${3:login_url}"
    name = "${4:name}"
    username = "${5:username}"
}