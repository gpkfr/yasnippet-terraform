# -*- mode: snippet -*-
# name: r_dynatrace_aws_credentials
# key: r_dynatrace_aws_credentials
# --

resource "dynatrace_aws_credentials" "${1:this}" {
    partition_type = "${2:partition_type}"
    tagged_only = "${3:tagged_only}"
}