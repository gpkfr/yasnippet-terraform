# -*- mode: snippet -*-
# name: r_dynatrace_user
# key: r_dynatrace_user
# --

resource "dynatrace_user" "${1:this}" {
    email = "${2:email}"
    first_name = "${3:first_name}"
    last_name = "${4:last_name}"
    user_name = "${5:user_name}"
}