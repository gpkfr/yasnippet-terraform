# -*- mode: snippet -*-
# name: r_statuscake_pagespeed_check
# key: r_statuscake_pagespeed_check
# --

resource "statuscake_pagespeed_check" "${1:this}" {
    check_interval = "${2:check_interval}"
    name = "${3:name}"
    region = "${4:region}"
}