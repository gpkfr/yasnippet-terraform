# -*- mode: snippet -*-
# name: r_cohesity_cloud_edition_cluster
# key: r_cohesity_cloud_edition_cluster
# --

resource "cohesity_cloud_edition_cluster" "${1:this}" {
    cluster_gateway = "${2:cluster_gateway}"
    cluster_name = "${3:cluster_name}"
    cluster_subnet_mask = "${4:cluster_subnet_mask}"
    dns_servers = "${5:dns_servers}"
    domain_names = "${6:domain_names}"
    node_ips = "${7:node_ips}"
    ntp_servers = "${8:ntp_servers}"
}