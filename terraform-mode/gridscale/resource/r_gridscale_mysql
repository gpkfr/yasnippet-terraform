# -*- mode: snippet -*-
# name: r_gridscale_mysql
# key: r_gridscale_mysql
# --

resource "gridscale_mysql" "${1:this}" {
    name = "${2:name}"
    performance_class = "${3:performance_class}"
    release = "${4:release}"
}