# -*- mode: snippet -*-
# name: r_gridscale_postgresql
# key: r_gridscale_postgresql
# --

resource "gridscale_postgresql" "${1:this}" {
    name = "${2:name}"
    performance_class = "${3:performance_class}"
    release = "${4:release}"
}