# -*- mode: snippet -*-
# name: r_gridscale_filesystem
# key: r_gridscale_filesystem
# --

resource "gridscale_filesystem" "${1:this}" {
    name = "${2:name}"
    performance_class = "${3:performance_class}"
    release = "${4:release}"
}