# -*- mode: snippet -*-
# name: r_tetration_user
# key: r_tetration_user
# --

resource "tetration_user" "${1:this}" {
    email = "${2:email}"
    first_name = "${3:first_name}"
    last_name = "${4:last_name}"
}