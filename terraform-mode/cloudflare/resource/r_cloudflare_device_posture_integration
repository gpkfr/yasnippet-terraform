# -*- mode: snippet -*-
# name: r_cloudflare_device_posture_integration
# key: r_cloudflare_device_posture_integration
# --

resource "cloudflare_device_posture_integration" "${1:this}" {
    account_id = "${2:account_id}"
    name = "${3:name}"
    type = "${4:type}"
}