# -*- mode: snippet -*-
# name: r_cloudflare_ruleset
# key: r_cloudflare_ruleset
# --

resource "cloudflare_ruleset" "${1:this}" {
    kind = "${2:kind}"
    name = "${3:name}"
    phase = "${4:phase}"
}