# -*- mode: snippet -*-
# name: r_cloudflare_argo_tunnel
# key: r_cloudflare_argo_tunnel
# --

resource "cloudflare_argo_tunnel" "${1:this}" {
    account_id = "${2:account_id}"
    name = "${3:name}"
    secret = "${4:secret}"
}