# -*- mode: snippet -*-
# name: r_boundary_credential_store_vault
# key: r_boundary_credential_store_vault
# --

resource "boundary_credential_store_vault" "${1:this}" {
    address = "${2:address}"
    scope_id = "${3:scope_id}"
    token = "${4:token}"
}