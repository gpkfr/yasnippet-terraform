# -*- mode: snippet -*-
# name: r_aviatrix_device_interface_config
# key: r_aviatrix_device_interface_config
# --

resource "aviatrix_device_interface_config" "${1:this}" {
    device_name = "${2:device_name}"
    wan_primary_interface = "${3:wan_primary_interface}"
    wan_primary_interface_public_ip = "${4:wan_primary_interface_public_ip}"
}