# -*- mode: snippet -*-
# name: r_aquasec_image
# key: r_aquasec_image
# --

resource "aquasec_image" "${1:this}" {
    registry = "${2:registry}"
    repository = "${3:repository}"
    tag = "${4:tag}"
}